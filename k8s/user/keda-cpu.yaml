apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: auth-keda
spec:
  scaleTargetRef:
    name: user-service

  minReplicaCount: 2
  maxReplicaCount: 10

  triggers:
  - type: cpu
    metadata:
      type: Utilization
      value: "50"
