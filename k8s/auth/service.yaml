apiVersion: v1
kind: Service
metadata:
  name: auth-service
  labels:
    app: auth-service   # 🔥 REQUIRED
spec:
  selector:
    app: auth-service
  ports:
    - name: http        # 🔥 VERY IMPORTANT
      port: 80
      targetPort: 3001
  type: NodePort
