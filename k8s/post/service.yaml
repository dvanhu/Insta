apiVersion: v1
kind: Service
metadata:
  name: post-service
  labels:
    app: post-service   # 🔥 REQUIRED
spec:
  selector:
    app: post-service
  ports:
    - name: http        # 🔥 VERY IMPORTANT
      port: 80
      targetPort: 3003
  type: NodePort
